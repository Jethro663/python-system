# Python System Agent Notes

## Scope Rules
- Keep the project aligned to `Nexora-Python-Final-Project-Checklist.docx`.
- Prioritize the core admin, teacher, and student web workflow before any optional polish.
- Use `C:\Users\jethr\Desktop\capstone-nest-react-lms` as the source of truth for behavior and workflow patterns that need to be copied or translated into Python.

## AI Features
- Do not implement AI features in this workspace.
- Any AI-related surface, helper, generator, mentor flow, extraction tool, or assistant page should be treated as `Coming Soon`.
- If an AI-facing route or UI is needed for completeness, render a simple `Coming Soon` page instead of building real AI behavior.
